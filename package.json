{
	"name": "obsidian-skysky-theme",
	"version": "1.0.0",
	"scripts": {
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"watch": "sass --no-source-map --watch src/index.scss theme.css --style=expanded"
	}
}